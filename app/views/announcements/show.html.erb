<p id="notice"><%= notice %></p>

<p>
  <h1>
    <%= @announcement.title %>
  </h1>
</p>

<p>
  <strong>Category:</strong>
  <%= @announcement.category %>
</p>

<p>
  <strong>Contents:</strong>
  <%= @announcement.contents %>
</p>

<p>
  <small>
    Created by 
    <%= @announcement.office_id %> on 
    <%= @announcement.created_at.in_time_zone("Central Time (US & Canada)").strftime("%B %d, %Y at %I:%M %p") %>.
  </small>
</p>

<p>
  <small>
  Last edited by 
  <%= @announcement.editor_id %> on 
  <%= @announcement.updated_at.in_time_zone("Central Time (US & Canada)").strftime("%B %d, %Y at %I:%M %p") %>.
  </small>
</p>

<%= link_to 'Edit', edit_announcement_path(@announcement) %> |
<%= link_to 'Back', announcements_path %>
